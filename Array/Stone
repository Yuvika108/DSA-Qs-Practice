#include <bits/stdc++.h>
#define ll long long int
using namespace std;

int main() {
	ll n, k;
	cin >> n >> k;
	
	ll v[n];
	for(int i=0; i<n; i++) cin >> v[i];
	
	if(k==0)
	    k=0;
	else if(k%2==0) 
	    k=2;
	else 
	    k=1;
	
	while(k--){
	ll MAX = *max_element(v, v+n);
    	for(int i=0; i<n; i++){
    	    v[i]=MAX-v[i];
    	}
	}
	
	for(int i=0; i<n; i++) cout << v[i] << " ";
	cout << "\n";
	
    return 0;
}
